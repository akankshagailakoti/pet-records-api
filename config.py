# Stores connection details
# Keeps credentials like host, user, and password safe and easy to change
# include placeholder values, not the real password when pushing it to GitHub

#Create your own config.py and fill these

HOST = "type your host name"
USER = "type your user"
PASSWORD = "type your password"  
